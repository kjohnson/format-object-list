{
    "name": "kjohnson/format-object-list",
    "description": "Formats an associative array into a JS parsable array of objects.",
    "type": "library",
    "license": "GPL-2.0-or-later",
    "minimum-stability": "dev",
    "scripts": {
        "test": "phpunit tests"
    },
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^9"
    },
    "autoload": {
        "psr-4": {
          "FormatObjectList\\": "src/"
        }
    }
}
